<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.TumascotaApp"
        tools:targetApi="31">

        <!-- Splash para verificar si ya se inicio sesión -->
        <activity
            android:name=".SplashActivity"
            android:exported="true"
            android:theme="@style/Theme.TumascotaApp">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Registro -->
        <activity android:name=".RegisterActivity" />

        <!-- Login -->
        <activity android:name=".LoginActivity" />

        <!-- Pantalla Home -->
        <activity android:name=".HomeActivity" />

        <!-- Registro de citas -->
        <activity android:name=".AgendaCitaActivity" />

        <!--Calendario del  veterinario -->
        <activity android:name=".AgregarMascotaActivity" />

        <!-- Mi Perfil -->
        <activity android:name=".PerfilActivity" />

        <!-- Veterinario -->
        <activity android:name=".VeterinarioActivity" />

        <!--Calendario del  veterinario -->
        <activity android:name=".CalendarioVeterinarioActivity" />



    </application>

</manifest>
